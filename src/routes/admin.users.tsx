import { createFileRoute } from "@tanstack/react-router";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { toast } from "sonner";
import { Plus, KeyRound, Shield, ShieldOff } from "lucide-react";
import { AdminShell } from "@/components/admin-shell";
import { getAdminUsers, createAdminUser, resetAdminPassword, setAdminStatus, changeOwnPassword, getCurrentAdmin } from "@/lib/api";

export const Route = createFileRoute("/admin/users")({ component: AdminUsersPage });
function AdminUsersPage() { const current = getCurrentAdmin(); const qc = useQueryClient(); const { data: users = [] } = useQuery({ queryKey: ["admin-users"], queryFn: getAdminUsers }); const [email, setEmail] = useState(""); const [nickname, setNickname] = useState(""); const [password, setPassword] = useState(""); const [ownPass, setOwnPass] = useState(""); if (current?.role !== "owner") return <AdminShell title="Admin Users"><div className="glass rounded-3xl p-8 text-center text-sm text-muted-foreground">Owner access required.</div></AdminShell>; return <AdminShell title="Admin Users"><div className="grid gap-6 lg:grid-cols-[.9fr_1.3fr]"><div className="space-y-6"><div className="glass rounded-3xl p-5"><h2 className="font-semibold">Add New Admin</h2><p className="mt-1 text-xs text-muted-foreground">Only owner can add admins. New admins cannot access this page.</p><div className="mt-4 space-y-3"><F label="Admin Email" value={email} onChange={setEmail} /><F label="Admin Nickname" value={nickname} onChange={setNickname} /><F label="Password" value={password} onChange={setPassword} type="password" /><button onClick={async () => { if (!email || !nickname || password.length < 10) return toast.error("Email, nickname and 10+ char password required"); await createAdminUser({ email, nickname, password, role: "admin" }); setEmail(""); setNickname(""); setPassword(""); qc.invalidateQueries({ queryKey: ["admin-users"] }); toast.success("Admin added"); }} className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-primary px-4 py-2 text-sm font-semibold text-primary-foreground shadow-glow"><Plus className="h-4 w-4" /> Add Admin</button></div></div><div className="glass rounded-3xl p-5"><h2 className="font-semibold">Change Own Password</h2><div className="mt-4 space-y-3"><F label="New Password" value={ownPass} onChange={setOwnPass} type="password" /><button onClick={async () => { if (ownPass.length < 10) return toast.error("Password must be at least 10 characters"); await changeOwnPassword(ownPass); setOwnPass(""); toast.success("Password changed"); }} className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-secondary px-4 py-2 text-sm font-semibold"><KeyRound className="h-4 w-4" /> Change Password</button></div></div></div><div className="glass rounded-3xl overflow-x-auto"><table className="w-full text-sm"><thead className="text-left text-xs uppercase text-muted-foreground"><tr><th className="px-4 py-3">Admin</th><th>Role</th><th>Status</th><th>Added By</th><th></th></tr></thead><tbody>{users.map((u) => <tr key={u.id} className="border-t border-border"><td className="px-4 py-3"><div className="font-semibold">{u.nickname}</div><div className="text-xs text-muted-foreground">{u.email}</div></td><td>{u.role}</td><td><span className={`rounded-full px-2 py-0.5 text-[10px] font-semibold uppercase ${u.isActive ? "bg-success/15 text-success" : "bg-destructive/15 text-destructive"}`}>{u.isActive ? "active" : "disabled"}</span></td><td className="text-xs text-muted-foreground">{u.createdByNickname || "system"}</td><td className="px-4 py-3 text-right"><div className="inline-flex gap-2"><button onClick={async () => { const pass = window.prompt(`New password for ${u.nickname}`); if (!pass) return; if (pass.length < 10) return toast.error("Password must be 10+ characters"); await resetAdminPassword(u.id, pass); toast.success("Password reset"); }} className="rounded-lg p-2 hover:bg-secondary"><KeyRound className="h-4 w-4" /></button>{u.email !== current.email && <button onClick={async () => { await setAdminStatus(u.id, !u.isActive); qc.invalidateQueries({ queryKey: ["admin-users"] }); toast.success(u.isActive ? "Admin disabled" : "Admin enabled"); }} className="rounded-lg p-2 hover:bg-secondary">{u.isActive ? <ShieldOff className="h-4 w-4 text-destructive" /> : <Shield className="h-4 w-4 text-success" />}</button>}</div></td></tr>)}</tbody></table></div></div></AdminShell>; }
function F({ label, value, onChange, type = "text" }: { label: string; value: string; onChange: (v: string) => void; type?: string }) { return <label className="block"><span className="text-xs text-muted-foreground">{label}</span><input type={type} value={value} onChange={(e) => onChange(e.target.value)} className="mt-1 w-full rounded-lg bg-input/70 px-3 py-2 text-sm ring-1 ring-border" /></label>; }
