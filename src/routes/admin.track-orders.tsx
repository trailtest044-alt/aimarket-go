import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { Search, ClipboardList } from "lucide-react";
import { AdminShell } from "@/components/admin-shell";
import { searchAdminOrders, formatMoney } from "@/lib/api";
import type { Order } from "@/lib/mock-data";

export const Route = createFileRoute("/admin/track-orders")({ component: AdminTrackOrders });

function AdminTrackOrders() {
  const [q, setQ] = useState("");
  const [status, setStatus] = useState<Order["status"] | "all">("all");
  const { data: orders = [], isLoading, refetch } = useQuery({ queryKey: ["admin-order-search", q, status], queryFn: () => searchAdminOrders(q, status), enabled: false });
  const [searched, setSearched] = useState(false);
  async function run() { setSearched(true); await refetch(); }
  return <AdminShell title="Track Orders"><div className="glass rounded-3xl p-6"><div className="flex items-center gap-2 text-sm font-semibold text-slate-800"><ClipboardList className="h-4 w-4 text-primary" /> Advanced order search</div><p className="mt-2 text-sm text-muted-foreground">Search by transaction ID, order ID, customer name, Gmail/email, phone/WhatsApp, product, status, or admin nickname.</p><div className="mt-5 grid gap-3 lg:grid-cols-[1fr_180px_auto]"><input value={q} onChange={(e) => setQ(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") run(); }} placeholder="Name, email, phone, Transaction ID, Order ID, product..." className="rounded-2xl bg-input/70 px-4 py-3 text-sm outline-none ring-1 ring-border focus:ring-primary" /><select value={status} onChange={(e) => setStatus(e.target.value as Order["status"] | "all")} className="rounded-2xl bg-input/70 px-4 py-3 text-sm outline-none ring-1 ring-border focus:ring-primary"><option value="all">All status</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="delivered">Delivered</option><option value="rejected">Rejected</option></select><button onClick={run} className="inline-flex items-center justify-center gap-2 rounded-2xl bg-gradient-primary px-5 py-3 text-sm font-semibold text-primary-foreground shadow-glow"><Search className="h-4 w-4" /> Search</button></div></div>{searched && <div className="mt-6 glass rounded-3xl overflow-x-auto"><table className="w-full text-sm"><thead className="text-left text-xs uppercase tracking-wider text-muted-foreground"><tr><th className="px-4 py-3">Order</th><th>Customer</th><th>Product</th><th>Payment</th><th>Amount</th><th>Status</th><th>Admin</th></tr></thead><tbody>{isLoading ? <tr><td colSpan={7} className="p-8 text-center text-muted-foreground">Searching...</td></tr> : orders.length === 0 ? <tr><td colSpan={7} className="p-8 text-center text-muted-foreground">No orders found.</td></tr> : orders.map((o) => <tr key={o.id} className="border-t border-border align-top"><td className="px-4 py-3"><div className="font-mono text-xs font-semibold">{o.id}</div><div className="mt-1 text-[11px] text-muted-foreground">{new Date(o.createdAt).toLocaleString()}</div><div className="mt-1 text-[11px]">Tx: <span className="font-mono">{o.transactionId}</span></div>{o.customerOrderRef && <div className="text-[11px]">Ref: <span className="font-mono">{o.customerOrderRef}</span></div>}</td><td><div className="font-semibold">{o.customerName}</div><div className="text-xs text-muted-foreground">{o.customerEmail}</div><div className="text-xs text-muted-foreground">{o.contact}</div></td><td>{o.productName}</td><td><div className="capitalize">{o.paymentMethod}</div><div className="text-xs text-muted-foreground">{o.paymentChannel}</div></td><td className="font-semibold">{formatMoney(o.amount, o.currency)}</td><td><span className="rounded-full bg-secondary px-3 py-1 text-xs font-semibold capitalize">{o.status}</span></td><td><div className="text-xs">{o.approvedByNickname && `Approved by ${o.approvedByNickname}`}</div><div className="text-xs">{o.deliveredByNickname && `Delivered by ${o.deliveredByNickname}`}</div><div className="text-xs">{o.rejectedByNickname && `Rejected by ${o.rejectedByNickname}`}</div></td></tr>)}</tbody></table></div>}</AdminShell>;
}
